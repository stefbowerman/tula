<div class="grid__item">
  <ul class="{{ global__basic_font_size }} list--reset p0 m0 mxn1 mtn1">
    {% for link in linklists[link_list].links %}
      <li class="">
        <a href="{{ link.url }}" class="footer__nav-link font-size--ml p1 inline-block mobile-nav__link color--{{ section_color }}-text ">
          <span class="footer__nav-link-span">{{ link.title }}</span>
        </a>
      </li>
    {% endfor %}
  </ul>
</div>